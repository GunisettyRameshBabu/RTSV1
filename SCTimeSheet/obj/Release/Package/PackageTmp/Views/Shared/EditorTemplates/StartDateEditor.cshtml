@*@( Html.Kendo().DateTimePicker()
              .Name("StartDate_#=ProjectId#")
              .Format("{0:dd MMM yyyy}")
              .Value("#=StartDate")
              .HtmlAttributes(new { data_bind = "value:StartDate" })

    )*@

@(Html.Kendo().DatePicker()
            .Name("StartDate")
            .Value("#=StartDate#")
             .Events(e =>
              {
                  e.Change("StartDateChange");
              })
            .HtmlAttributes(new { style = "width: 50%", title = "Select Start Date" , @required = "required" })
           .Format("dd MMM yyyy")
)
